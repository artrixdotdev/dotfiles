// Window rules let you adjust behavior for individual windows.
// Find more information on the wiki:
// https://github.com/YaLTeR/niri/wiki/Configuration:-Window-Rules

// Work around WezTerm's initial configure bug
// by setting an empty default-column-width.
window-rule {
    // This regular expression is intentionally made as specific as possible,
    // since this is the default config, and we want no false positives.
    // You can get away with just app-id="wezterm" if you want.
    match app-id=r#"^org\.wezfurlong\.wezterm$"#
    default-column-width {}
}

// Open the Firefox picture-in-picture player as floating by default.
window-rule {
    // This app-id regular expression will work for both:
    // - host Firefox (app-id is "firefox")
    // - Flatpak Firefox (app-id is "org.mozilla.firefox")
    match app-id=r#"firefox$"# title="^Picture-in-Picture$"
    open-floating true
}

// Keep Bitwarden popups floating and centered by default.
window-rule {
    match app-id=r#"(?i)bitwarden"#
    match title=r#"(?i)bitwarden"#

    open-floating true
    open-focused true
    block-out-from "screen-capture"
}

// Block out password managers from screen capture.
window-rule {
    match app-id=r#"^org\.keepassxc\.KeePassXC$"#
    match app-id=r#"^org\.gnome\.World\.Secrets$"#

    block-out-from "screen-capture"
}

// Block out notification pop-ups from screen capture.
layer-rule {
    match namespace=r#"(?i)(notification|notify)"#

    block-out-from "screen-capture"
}

// Example: enable rounded corners for all windows.
// (This example rule is commented out with a "/-" in front.)
window-rule {
   geometry-corner-radius {{mul settings.sizing.base 2}}
   clip-to-geometry true
}

// Workspace routing for task-specific apps.
// Ghostty and Zen are intentionally excluded so they can still be opened anywhere.
window-rule {
   match app-id=r#"^vesktop$"#
   open-on-workspace "chat"
}

window-rule {
   match app-id=r#"^spotify$"#
   open-on-workspace "music"
}

window-rule {
   match app-id=r#"^(io\.missioncenter\.MissionCenter|missioncenter)$"#
   open-on-workspace "monitor"
}

window-rule {
   match app-id=r#"^(dev\.zed\.Zed(-Preview)?|zed|zeditor)$"#
   open-on-workspace "code"
}

window-rule {
   match app-id=r#"^microsoft-edge-dev$"#
   open-on-workspace "work"
}

window-rule {
   match app-id=r#"^(notion|notion-app)$"#
   open-on-workspace "work"
}

window-rule {
   match app-id=r#"^(Cron|notion-calendar)$"#
   open-on-workspace "work"
}
